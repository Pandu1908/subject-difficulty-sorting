# subject-difficulty-sorting
A Python program that sorts subjects based on their difficulty values using a dictionary and the sorted() function. It uses items(), a lambda function to select difficulty as the sorting key, and reverse=True to arrange subjects from highest to lowest difficulty before displaying the priority list.
